image:
  file: ../../hocus.Dockerfile
  buildContext: ../../tmp_resources
tasks:
  - name: Setup git
    init: |
      git config --global --add user.email gorbak25@gmail.com && 
      git config --global --add user.name "Grzegorz Uriasz"
    # Ok for some reason this is required for now
    command: sleep 0.1
  - name: hello world
    init: sleep 0.1
    command: sleep 0.1
  - name: second task
    init: sleep 0.1
    command: sleep 0.1

image:
  file: ../../hocus.Dockerfile
  buildContext: ../../tmp_resources
tasks:
  - name: Setup git
    init: |
      git config --global --add user.email gorbak25@gmail.com && 
      git config --global --add user.name "Grzegorz Uriasz"
    commandShell: fish
    command: yarn
